---
title: Congresses
description: Lead retrieval events, attendees, business cards — matching src/domains/congresses.ts.
---

**Public surface (`congresses`):**  
`list` · `get` · `getAttendees` · `getAttendee` · `getBusinessCards` · `getBusinessCard`

Failures throw **`MobileLockerError`**.

## Events

```ts
list(): Promise<Event[]>
get(eventID: number): Promise<Event>
```

```js
import mobilelocker from '@mobilelocker/javascript-sdk'

const events = await mobilelocker.congresses.list()
const event = await mobilelocker.congresses.get(eventID)
```

## Attendees

```ts
getAttendees(eventID: number): Promise<Attendee[]>
getAttendee(attendeeID: string): Promise<Attendee>
```

```js
const attendees = await mobilelocker.congresses.getAttendees(eventID)
const attendee = await mobilelocker.congresses.getAttendee(attendeeID)
```

## Business cards

```ts
getBusinessCards(): Promise<BusinessCard[]>
getBusinessCard(cardID: string): Promise<BusinessCard>
```

```js
const cards = await mobilelocker.congresses.getBusinessCards()
const card = await mobilelocker.congresses.getBusinessCard(cardID)
```

## Related

- [Scanner](scanner/) — capture badge/card (iOS)
- [Search](search/) — includes `attendees` and `business_cards`

## API reference

- Module: [`congresses`](../api/variables/congresses/)
- Types: [`Event`](../api/interfaces/event/), [`Attendee`](../api/interfaces/attendee/), [`BusinessCard`](../api/interfaces/businesscard/)
